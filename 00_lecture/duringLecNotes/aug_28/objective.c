#include <stdio.h>
#include <stdlib.h>

// 1.) Program #1
// 2.) struct 
// 3.) malloc
	// a.) static vs dynamic allocation
	// b.) array of structs statically allocated
	// c.) array of structs dynamically allocated











	/* 3 syntax issues
	*  - Index of an array accessed via square brackets
	*  - If a is struct we access components of a using dot
	*  - If a is a ptr to a struct we access the component using the
	*    arrow ->
	*/
	/*
	*  pointer arithmetic
	*	- If a is a ptr, *a is the item a is pointing to.
	*   - If a is a variable, &a is the memory address(ptr)
	*	
	*/
